const user = {
    userName: "ルキア",
    userAge: 20,
    userFavorite: ["ゲーム", "音楽を聴く"]
};

console.log(`名前：${user.userName}`);
console.log(`年齢：${user.userAge}歳`);
console.log(`趣味：${user.userFavorite[0]}, ${user.userFavorite[1]}  など`);